class third
{
    public static void main(String args[])
    {
        System.out.printf("so what do we do");
        /*Does not add a newline unless specified.
          Uses format specifiers.
          System.out.printf("Value: %d", 10); */
        System.out.print("here is nothing to do");
        /* Does not move to a new line. 
           Cursor stays on the same line. */
        System.out.println("there is a lot of to do");
        /* Appends a newline after printing.
           Cursor moves to the next line. */
        

    }
}